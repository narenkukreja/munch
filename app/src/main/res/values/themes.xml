<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.MunchForReddit" parent="Theme.MaterialComponents.DayNight.NoActionBar" />

    <!-- Translucent theme for activities with swipe-back -->
    <style name="Theme.MunchForReddit.Translucent" parent="Theme.MunchForReddit">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowIsFloating">false</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
    </style>

</resources>
